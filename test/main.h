/* Causes the test to get a main() function.

Included by the <test/kit.h>. Adds a main function which accepts standard test
arguments and then runs unit tests, fixture by fixture, creating a report. */
