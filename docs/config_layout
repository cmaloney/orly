/usr/share/bit

/jobs/
  compile_c_family.so

/presets/
  c++11.bit
  debug.mixin.bit
  release.mixin.bit
  /jobs/{name}/
    debug.mixin.bit
    release.mixin.bit

TODO: What is the linux standard location for os-provided config
For now cheating
/distro/
  dependencies  # how to find / fetch / etc. distro deps of packages
  dep_map.bit # system header -> link argument mapping
  dep_map/
    { program/lib specific one }
