#pragma once

#include <bit/config.h>
#include <bit/naming.h>

namespace Bit {

  class TFileConfig {
    TJobConfig JobConfig;

  };

  class TFile {
    public:
    TFile(TRelPath &&path, )

  };
}
